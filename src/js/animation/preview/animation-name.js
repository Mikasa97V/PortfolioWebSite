// const onEntry = (entry) => {
//     entry.forEach(change => {
//       if (change.isIntersecting) {
//        change.target.classList.add('info-name_show');
//       } else {
//          change.target.classList.remove('info-name_show');
//       }
//     });
//   }



  
//   let options = { threshold: [0.5] },
//       observer = new IntersectionObserver(onEntry, options),
//       nameWrap = document.querySelector('.info-name');

//   observer.observe(nameWrap);

// //   console.log(nameWrap.getBoundingClientRect());
